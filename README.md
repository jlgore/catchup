Download the catchup script:

`wget https://github.com/jlgore/catchup`

Change the scripts permissions:

`chmod +x appdevelopers.sh`
`chmod +x dbadmin.sh`
Execute the script:

`./appdevelopers.sh`
`./dbadmin.sh`
